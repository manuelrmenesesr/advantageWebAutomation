import { AriaObject } from "../../utils/types.util";

const role_btnAddToCart: AriaObject = {
  role: "button",
  options: { name: "ADD TO CART" },
};

export const productLocators = {
  role_btnAddToCart: role_btnAddToCart,
};
